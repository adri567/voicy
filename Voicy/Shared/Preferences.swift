import Foundation

nonisolated enum Preferences {
    enum Key {
        static let showTranscript = "dev.showTranscript"
        static let correctionEnabled = "dev.correctionEnabled"
        static let transcriptionEngine = "dev.transcriptionEngine"
    }
}
